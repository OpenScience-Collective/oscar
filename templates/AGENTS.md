# Your Tool: notes for agents

<!--
  OSCAR starter: AGENTS.md   (standard: https://agents.md)
  Put this at the root of your repository, or the root of your site.
  Write it for a capable newcomer who will act on it: an agent or a new contributor.
  Keep it short, concrete, and example-led. Delete this comment before shipping.
-->

## What this is
One paragraph: what the tool does, and the single most common task someone uses it for.

## Install / access
The exact command or URL to get started.
```
pip install your-tool     # or: the download link, or the API base URL
```

## The commands that matter
The three to five entry points a newcomer actually needs, each with a one-line purpose.
```
your-tool run <input>        # the main action
your-tool validate <input>   # check before running
your-tool --help             # full reference
```

## A worked example
End to end, with real (small) inputs and the expected result.
```
your-tool run example.dat --format json
# -> writes result.json with fields a, b, c
```

## Conventions and gotchas
The two or three things newcomers always get wrong
(units, required order of steps, a flag that must be set).

## Where to learn more
- Docs: https://your-tool.org/docs
- API reference: https://your-tool.org/api
- How to cite: https://your-tool.org/about
